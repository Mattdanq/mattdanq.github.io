import TopBar from "../components/topBar";

const About = () => {
    return(
        <div>
            <TopBar />
            <h2>If you see this the about page correctly loading on github pages</h2>
        </div>
    );
}

export default About;